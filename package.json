{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "lerna exec --stream -- npm install",
    "start": "lerna exec --parallel --stream -- npm start",
    "clean": "lerna exec --parallel --stream -- npm run clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "3.20.2"
  }
}
